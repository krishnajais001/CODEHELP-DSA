#include <iostream>
#include <vector> 
using namespace std;
int main()

{ 
/* 
    // Shift Element by one position
	vector <int> arr = {10,20,30,40} ;
	int size = arr.size();

	int lastElement = arr[size-1] ;

	for(int i = size-1 ; i >= 0 ; i--) {
		if(i!=0){
		    arr[i] = arr[i-1];
		}else{
		    	arr[0] = lastElement ;
		}
	}


	for(int i = 0  ; i < size ; i++) {
		cout<< arr[i]<<endl ;
	}
*/
   
/*   
// Shift Element by two position
    vector <int> arr = {10,20,30,40,50} ;
    int size = arr.size();
    int lastElement = arr[size-1] ;
    int secondLastElement = arr[size-2] ;

    for(int i = size-1 ; i >= 1 ; i--){
        arr[i] = arr[i-2];
    }
    
    arr[0] = lastElement ;
    arr[1] = secondLastElement ;
    
     for(int i = 0  ; i < size ; i++){
       cout<< arr[i] << endl ;
    }
*/

    return 0;
}